# ── Frontend (Vercel) ────────────────────────────────────────────────────────
# Server env: Next.js API routes proxy to Render (recommended — no CORS)
BACKEND_URL=https://kospi-screener-api.onrender.com

# Do NOT set on Vercel (browser direct calls cause CORS/network errors):
# NEXT_PUBLIC_API_URL=

# ── Backend (Render / Docker) ────────────────────────────────────────────────
DEBUG=false

# Primary frontend origin (also merged into CORS allowlist)
FRONTEND_URL=https://your-app.vercel.app

# Extra allowed origins — comma-separated (preview URLs, custom domains)
CORS_ORIGINS=https://your-app.vercel.app,https://your-app-git-main.vercel.app

# Allow all Vercel preview deployments (*.vercel.app)
CORS_ORIGIN_REGEX=https://.*\.vercel\.app

# Optional Bayesian / universe overrides
# PRIOR_UP_PROB=0.52
# UNIVERSE_LIMIT=50

# pykrx KRX API (recommended for production data quality)
# KRX_ID=your_id
# KRX_PW=your_password
